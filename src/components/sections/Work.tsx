import * as React from "react";

const Work: React.FC = () => {
  return (
    <div
      style={{
        height: "500px",
        color: "red",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        borderStyle: "solid",
      }}
    >
      WORK SECTION! *in progress*
    </div>
  );
};

export default Work;
