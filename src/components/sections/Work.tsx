import * as React from "react";

const Work: React.FC = () => {
  return (
    <div
      style={{
        height: "850px",
        color: "red",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <h1 id="work">WORK SECTION! *in progress*</h1>
    </div>
  );
};

export default Work;
